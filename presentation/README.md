# Presentation

Slides are Markdown-first and should be exported after the system is running and real screenshots exist.

Suggested flow:

```bash
make db-up
make etl-demo
make mongo-load
make validate-final
```

Then capture dashboard/API screenshots and export with Marp or Slidev.

Do not present placeholder screenshots as real evidence.
