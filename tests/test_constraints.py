def test_constraints_score_and_confidence() -> None:
    from tests.test_final_project_static import (
        test_score_and_confidence_constraints_are_declared,
    )

    test_score_and_confidence_constraints_are_declared()
