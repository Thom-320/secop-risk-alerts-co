from __future__ import annotations

from src.scoring.score_processes import build_process_scores, main

__all__ = ["build_process_scores", "main"]

# Legacy compatibility wrapper. The active flow uses `score_processes`.
